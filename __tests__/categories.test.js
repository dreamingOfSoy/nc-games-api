const { getAllCategories } = require("../controllers/categoriesController");

describe("getAllCategories()", () => {
  it.todo(
    "200 - should respond with correct status code and contain correct feilds (slug, description)"
  );
});
